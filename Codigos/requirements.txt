numpy
opencv-python
requests
alpyca>=3.1.2,<4
pyvisa
pyvisa-py
pyusb
ids_peak
ids_peak_ipl

# Verificacao de nomes indefinidos e erros de sintaxe (regras em pyproject.toml).
# A suite de testes roda o Ruff; sem ele, essa checagem e apenas pulada.
ruff
